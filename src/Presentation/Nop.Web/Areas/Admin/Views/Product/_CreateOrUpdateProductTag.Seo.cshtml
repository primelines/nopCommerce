@model ProductTagModel

<div class="card-body">
    <div class="cards-group">
        @(await Html.LocalizedEditorAsync<ProductTagModel, ProductTagLocalizedModel>("producttag-seo-localized",
        @<div>
            <div>
                <input type="hidden" asp-for="@Model.Locales[item].LanguageId" />
            </div>
        </div>
            ,
        @<div>
        </div>))
    </div>

</div>